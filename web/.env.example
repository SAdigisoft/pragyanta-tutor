# Mock-first by default; set to false to use the FastAPI adapter.
VITE_USE_MOCKS=true
VITE_API_BASE_URL=http://localhost:8000

# Hide only when the screens are approved for production.
VITE_SHOW_DEMO_SWITCHER=true
